services:
  web:
    build: .
    container_name: mood-recommender
    ports:
      - "5000:5000"
    environment:
      FLASK_ENV: development
      SECRET_KEY: change-this-in-production
    restart: unless-stopped
